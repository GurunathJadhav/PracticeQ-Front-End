import { PatientAppointments } from './patient-appointments';

describe('PatientAppointments', () => {
  it('should create an instance', () => {
    expect(new PatientAppointments()).toBeTruthy();
  });
});
