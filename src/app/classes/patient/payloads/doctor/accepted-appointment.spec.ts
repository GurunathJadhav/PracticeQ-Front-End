import { AcceptedAppointment } from './accepted-appointment';

describe('AcceptedAppointment', () => {
  it('should create an instance', () => {
    expect(new AcceptedAppointment()).toBeTruthy();
  });
});
